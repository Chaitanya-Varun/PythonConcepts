names=["Chai","Va"]
# for name in names:
# 	#print("Hello there, "+name) #This is basic and readable
# 	print(" ".join(["Hello there,",name])) #tihs is same but has more complexity,but is more efficient

#print(','.join(names))#for this kind of things this is good
#use join for concatenating strings

import os
# for getting path of the file use os.path and then join with the required files

#kk,understood os.paths.join(), it basically keep up the order with which we are concatenating

#String formating
#string.format(args)